<p>You aren't a member of any organisations.</p>
<p>Join an existing one or create a new one.</p>

<h2>Organisations</h2>
<% if @organisations.length > 0 %>
  <ul>
    <% @organisations.each do |organisation| %>
      <li>
        <%= organisation.name %> 
        <%= link_to "Edit", edit_organisation_path(organisation) %> 
        <%= link_to "Join", organisation_join_path(organisation, current_user), method: :patch %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>There are no organisations.</p>
  <p>Create a new one using the form below.</p>
<% end %>

<h2>Create Organisation</h2>
<%= form_for @organisation do |f| %>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :hourly_rate %><br />
    <%= f.number_field :hourly_rate, step: 0.01 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>