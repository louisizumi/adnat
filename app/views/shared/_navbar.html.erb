<nav class="flex--space-between">
  <% if current_user %>
    <h1><%= link_to "Adnat", current_user.organisation ? home_path : organisations_path %></h1>
    <p>Logged in as <%= current_user.full_name %> <%= link_to "Log Out", destroy_user_session_path, method: :delete %></p>
  <% else %>
    <h1><%= link_to "Adnat", home_path %></h1>
  <% end %>
</nav>