<nav class="flex--space-between">
  <h1><%= link_to "Adnat", organisations_path, id: "logo" %></h1>
  <% if current_user %>
    <p>Logged in as <%= current_user.full_name %> <%= link_to "Log Out", destroy_user_session_path, method: :delete, class: "btn btn--red" %></p>
  <% end %>
</nav>